// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    API_BASE_PATH: 'http://localhost:3000/federaciodefogueres/contador-playbacks-api/1.0.0',
    firebase: {
      apiKey: "AIzaSyDHqm8LmUHebUWewJj-joJiyZEjGiItSB8",
      authDomain: "contador-playbacks.firebaseapp.com",
      projectId: "contador-playbacks",
      storageBucket: "contador-playbacks.appspot.com",
      messagingSenderId: "27950022924",
      appId: "1:27950022924:web:82e2c0c5a769466a50c929",
      measurementId: "G-6QDHJMLHDV"
    }
  };
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/dist/zone-error';  // Included with Angular CLI.